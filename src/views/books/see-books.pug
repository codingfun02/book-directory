extends ../layout/layout.pug

block content
  ul.main__book-list
    each book in books
      li
        a(href=`/books/${book._id}`)
          | #{book.title}
        span  (Added By 
          a(href="#").bold #{book.owner.username}
          | )
    else
      li Can't find any books